export class DoggyBag {
    constructor() {
        //TODO: Start up services, allocate space for CRC table etc
    }

    /*
     * Docstring & Folder structure
     */
    public generateZip(files: Object): Promise<Blob> {
        return Promise.resolve(new Blob());
    }
}